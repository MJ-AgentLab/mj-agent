# GENERATED -- do not edit this file. Owned 100% by scripts/sdd/agents_sync.py
# (emitter B; dual-agent-compat v5 S2 #330, ADR-036 D-011/D-012).
# Derived from .mcp.json (A14 hard-stop source, human-maintained) filtered by the
# manifest `mcp` per-server tiers (D-013) + `codex.posture` (D-017) in
# sdd/development-agent.yml. Secrets are referenced BY NAME via `env_vars`
# (Codex TOML has no ${VAR} interpolation; Codex sanitizes MCP child env and
# `env_vars` inherits the named variables from the parent environment) --
# literal credentials must never appear here (G7 scans this file; V11 blocks drift).
# To change: edit the SOURCE through its own gate, then run
# `python scripts/sdd/agents_sync.py sync` and commit config + .agents.lock.json.

approval_policy = "on-request"
sandbox_mode = "workspace-write"
project_doc_max_bytes = 65536

[mcp_servers.github]
command = "cmd"
args = ["/c", "npx", "-y", "@modelcontextprotocol/server-github"]
env_vars = ["GITHUB_PERSONAL_ACCESS_TOKEN"]

[mcp_servers.pg-mj-agent-memory-dev]
command = "cmd"
args = ["/c", ".claude\\scripts\\pg-server-start.cmd", "MJ_AGENT_PG_MEMORY_DEV_URL"]
env_vars = ["MJ_AGENT_PG_MEMORY_DEV_URL"]

[mcp_servers.pg-mj-agent-memory-prod-lan]
command = "cmd"
args = ["/c", ".claude\\scripts\\pg-server-start.cmd", "MJ_AGENT_PG_MEMORY_PROD_LAN_URL"]
env_vars = ["MJ_AGENT_PG_MEMORY_PROD_LAN_URL"]

[mcp_servers.pg-mj-agent-memory-prod-wan]
command = "cmd"
args = ["/c", ".claude\\scripts\\pg-server-start.cmd", "MJ_AGENT_PG_MEMORY_PROD_WAN_URL"]
env_vars = ["MJ_AGENT_PG_MEMORY_PROD_WAN_URL"]

[mcp_servers.pg-mj-agent-memory-test-lan]
command = "cmd"
args = ["/c", ".claude\\scripts\\pg-server-start.cmd", "MJ_AGENT_PG_MEMORY_TEST_LAN_URL"]
env_vars = ["MJ_AGENT_PG_MEMORY_TEST_LAN_URL"]

[mcp_servers.pg-mj-agent-memory-test-wan]
command = "cmd"
args = ["/c", ".claude\\scripts\\pg-server-start.cmd", "MJ_AGENT_PG_MEMORY_TEST_WAN_URL"]
env_vars = ["MJ_AGENT_PG_MEMORY_TEST_WAN_URL"]

[mcp_servers.playwright]
command = "cmd"
args = ["/c", "npx", "-y", "@playwright/mcp@latest"]

[mcp_servers.serena]
command = "cmd"
args = ["/c", "uv", "tool", "run", "--from", "git+https://github.com/oraios/serena", "serena", "start-mcp-server", "--context", "codex", "--enable-web-dashboard", "false", "--project-from-cwd"]
