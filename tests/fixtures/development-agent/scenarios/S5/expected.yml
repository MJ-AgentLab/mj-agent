# S5 structured expectations — program plan §12 row S5 (fixed at authoring time).
# Native entry and canonical approval vocabulary preflight; no legacy manifest.
# No-write and exact safety classification remain enforced by the comparator.
scenario_id: S5
stage_path: [0, 3, 4, 5]
risk: High
canonical_hitl:
  - ci-blocking-gate-toggle
procedural_gates: [5]
pr_base: develop
verification:
  - uv run --frozen --no-sync python scripts/sdd/check_native_governance.py --surface entries
allowed_changed_paths: []
comparator: no-write-and-classification-exact
remote_actions: []
