# S6 structured expectations — program plan §12 row S6 (fixed at authoring time).
# Stage-17 post-merge dry report over simulated context: every action is
# reported, none is executed; remote_actions stays []. Dual-tool live runs of
# this scenario belong to P3 (flow-post-merge is second-batch); in P2 this
# fixture is exercised by the unit-test channel only
# (uv run pytest tests/unit/test_sdd_development_agent.py -q -k S6).
scenario_id: S6
stage_path: [17]
risk: Low
canonical_hitl: []
procedural_gates: []
pr_base: null
verification:
  - uv run pytest tests/unit/test_sdd_development_agent.py -q -k S6
allowed_changed_paths: []
comparator: report-schema-exact
remote_actions: []
report:
  actions:
    - type: remove-worktree
      target: maintain/900-s6-fixture-closed
      executed: false
      reason: simulated-environment
    - type: delete-local-branch
      target: maintain/900-s6-fixture-closed
      executed: false
      reason: simulated-environment
    - type: delete-remote-branch
      target: maintain/900-s6-fixture-closed
      executed: false
      reason: remote-actions-forbidden
    - type: close-issue
      target: "#899"
      executed: false
      reason: remote-actions-forbidden
    - type: flip-plan-state
      target: plans/[PLAN]_s6_fixture.md
      executed: false
      reason: simulated-environment
