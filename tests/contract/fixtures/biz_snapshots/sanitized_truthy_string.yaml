# Synthetic NEGATIVE snapshot fixture (Epic #499 PR-0c) - must be REJECTED.
# Defect: sanitized is the STRING "true", not boolean true. A truthy value is
# not an attestation; strict identity check must reject it.
schema_version: schema-v1
captured_at: "2026-08-13T00:00:00+00:00"
provenance: sanctioned-agent-tool-chain
sanitized: "true"
payload:
  schemas:
    biz_dws:
      tables: {}
    biz_dwd:
      tables: {}
