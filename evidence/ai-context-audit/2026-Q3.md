---
type: ai-context-audit
cycle: 2026-Q3
auditor: "ai-agent (claude-opus-4-8[1m] via claude-code; HITL-supervised by ranzuozhou)"
scope:
  - root-claude-md
  - subdir-claude-md
  - claude-skills-inventory
  - freeze-surface-hashes
  - claude-settings-hooks
findings_summary: "catch-up entry (Q3 逾期 15 日 < 30 日门槛，gap 自愿留痕); 冻结面 8/8 drift-clean vs contract.yml — 零违规; 面集按新推导规则 15→23; A6 提醒机制经证实已静默失效 (M-FU-AI-AUDIT-2026-Q3 从未注册)"
content_hash_snapshot:
  CLAUDE.md: 511670039e403423
  capabilities/CLAUDE.md: ba11a07d5284b172
  docker/CLAUDE.md: 7ffdb503c01e0626
  src/mj_agent/CLAUDE.md: 44c81b2e7ff47b2e
  tests/CLAUDE.md: 738e0c1014c0eccf
  src/mj_agent/skills/biz-domain-context/SKILL.md: 0c70f7f311bc611b
  src/mj_agent/skills/biz-schema-exploration/SKILL.md: 52faf22c067d0194
  src/mj_agent/skills/mj-ddd-semantics/SKILL.md: a4e7f3a735c8abcf
  src/mj_agent/skills/monthly-report/SKILL.md: 808de042bbd475a7
  src/mj_agent/skills/probe-fixture/SKILL.md: a1942465e72937c8
  src/mj_agent/skills/qcm-analysis/SKILL.md: 047a614118133438
  src/mj_agent/skills/query-optimization/SKILL.md: 7ebcbe71f1310dda
  src/mj_agent/skills/query-writing/SKILL.md: 34e0aa18ee68a07a
  src/mj_agent/skills/safe-sql-analysis/SKILL.md: 749ed640fbcce91b
  src/mj_agent/prompts/system.md: 994d4a2d7fd3677f
  .claude/skills/mj-agent-infra-app-start/SKILL.md: 9ab21e612642ee92
  .claude/skills/mj-agent-infra-app-stop/SKILL.md: 2ed0c015e41e97aa
  .claude/skills/mj-agent-infra-docker-compose/SKILL.md: 3c68d460960e8f40
  .claude/skills/mj-agent-infra-env-setup/SKILL.md: 37ebae50b47398a0
  .claude/skills/mj-agent-infra-env-teardown/SKILL.md: 8963f81778301055
  .claude/skills/mj-agent-infra-llm-endpoint-probe/SKILL.md: 5e53851599a3e3dc
  .claude/skills/mj-agent-infra-storage-stack/SKILL.md: b4e4ec4dce74f04c
  .claude/skills/mj-agent-infra-studio-probe/SKILL.md: fcb36d85ff40bd7f
---

# AI-Context Audit — 2026-Q3 (Catch-up Cycle)

## §1 Scope + 本期性质

Snapshot 取自 `develop @ a576eea`（2026-07-16；= flip PR #346 合入后的 develop HEAD）。
本期为 **catch-up entry**：到期 2026-07-01，实际执行 2026-07-16 → **逾期 ~15 日**。

- **逾期处置**：`SCHEMA.md §3` 的 lapse policy 门槛是 **> 30 日** MUST 记 gap。本期 15 日
  **未达**该门槛 → 记 gap 属**自愿**。仍然记录，理由见 §6 F7：逾期的**成因**（提醒机制失效）
  比逾期本身更值得留痕。
- **面集**：首次按 `SCHEMA.md §2.1` **推导规则**（本切片同期落地）现场推导 = **23 面**
  （必停轨 18 + CLAUDE.md 轨 5），上期 15 面。差额来源见 §6 F6。
- 上游切片：issue #347（dual-agent-compat v5 第十切片）；同 PR 内另修
  `policies/ci-gates.md` §5/§4（见 §6 F8）+ `SCHEMA.md` §2.1（见 §6 F3）。

**算法自证（先复现、再新算）**：按 `SCHEMA.md §2.1` canonical regex-strip 实现复算 Q2 的 15
面，其中 **4 面逐字复现** Q2 记录值（`safe-sql-analysis` / `biz-domain-context` /
`qcm-analysis` / `system.md`）→ 证明本期实现即 canonical 算法，故下文所有 DRIFT 均为**真实
内容变更**，非实现差异。

## §2 CLAUDE.md Inventory（5 files；plain SHA-256）

| Path | Lines | Plain SHA-256 (前 16 hex) | vs Q2 |
|---|---:|---|---|
| `CLAUDE.md` | 170 | `511670039e403423` | **DRIFT**（Q2 `998d8d13c4b5ad9a`，610 行） |
| `src/mj_agent/CLAUDE.md` | 86 | `44c81b2e7ff47b2e` | **DRIFT**（Q2 `b2a122ed780efb78`，83 行） |
| `capabilities/CLAUDE.md` | 118 | `ba11a07d5284b172` | **DRIFT**（Q2 `158553738c8eae64`，114 行） |
| `tests/CLAUDE.md` | 78 | `738e0c1014c0eccf` | **DRIFT**（Q2 `bc1f5a5e8505f425`，74 行） |
| `docker/CLAUDE.md` | 102 | `7ffdb503c01e0626` | **DRIFT + 路径重命名**（Q2 `infra/docker/CLAUDE.md` = `c2d34d71485a1df5`，98 行） |

5/5 drift —— 判定见 §6 F4（**预期内**，非违规：CLAUDE.md 无冻结机制）。

## §3 `.claude/skills/` Inventory（37 SKILLs；Q2 = 34）

| Family | Count | Δ vs Q2 |
|---|---:|---|
| doc | 6 | — |
| flow | 10 | +1 |
| git | 9 | — |
| infra | **8** | **+2**（`app-start` / `app-stop`，#304；**均为必停冻结面** —— 见 §5） |
| runtime | 4 | — |
| **合计** | **37** | **+3** |

> `SCHEMA.md §4` 的「34」是 **2026-Q2 当时**事实、已标注为历史条款；本期起 skills 清点
> 不再作为规范常量（同 §2.1 推导纪律）。

## §4 `.claude/settings.json` 快照

| 项 | 2026-Q3 | 2026-Q2 | Δ |
|---|---|---|---|
| File SHA-256（前 16 hex） | `9d0c7670dbd478e2` | `89333565f78cb2a7` | **DRIFT** |
| `permissions.allow` | **24** | 8 | +16 |
| `permissions.deny` | **9** | 6 | +3 |
| `permissions.ask` | **5** | — （Q2 schema 无此档） | **新档** |
| `hooks.PreToolUse` | 1（`guard-git-workflow.ps1`，matcher `Bash`） | 1 | — |
| `hooks.Stop` | 1（`on-stop.ps1`） | 1 | — |
| `enabledPlugins` | `{}` | `{}`（空） | — |

**`ask` 档是本期最重要的结构变化**：ADR-034（2026-06-20）把 5 项必停面由 `deny` 物理硬锁
改为 `ask` 逐写拍板门。Q2 的 schema **无 `ask` 概念** → 该档在本期首次入账。
`deny` 档今日**不含**任何必停文件（仅 `.env` / `secrets*.enc` / 不可逆 `Bash` 破坏面）。

`permissions.ask` 5 条（= 5 项必停面）：
`Edit(./src/mj_agent/tools/sql/guardrail.py)` · `Edit(./src/mj_agent/tools/sql/precheck.py)` ·
`Edit(./src/mj_agent/prompts/system.md)` · `Edit(./src/mj_agent/biz_catalog/qcm_catalog.yaml)` ·
`Edit(./src/mj_agent/skills/**/SKILL.md)`

## §5 必停轨 drift check（18 面 vs contract）

### 5.1 冻结面（8 infra skills）—— **8/8 drift-clean vs contract；零违规**

逐项复算 body regex-strip hash 与 `capabilities/infrastructure/mcp-server-governance/contracts/claude-skill.contract.yml`
的 `body_content_hash` 比对：

| Skill | frozen_at | vs contract | vs Q2 |
|---|---|---|---|
| `mj-agent-infra-app-start` | 2026-07-08 | **OK** | 新面（#304） |
| `mj-agent-infra-app-stop` | 2026-07-08 | **OK** | 新面（#304） |
| `mj-agent-infra-docker-compose` | 2026-07-13 | **OK** | DRIFT → 已再冻结 |
| `mj-agent-infra-env-setup` | 2026-07-13 | **OK** | DRIFT → 已再冻结 |
| `mj-agent-infra-env-teardown` | 2026-07-08 | **OK** | DRIFT → 已再冻结 |
| `mj-agent-infra-llm-endpoint-probe` | 2026-07-13 | **OK** | DRIFT → 已再冻结 |
| `mj-agent-infra-storage-stack` | 2026-06-08 | **OK** | DRIFT → 已再冻结 |
| `mj-agent-infra-studio-probe` | 2026-07-08 | **OK** | DRIFT → 已再冻结 |

**判定**：6 面相对 Q2 漂移，但**全部**匹配 contract 现记 `body_content_hash`，且各自
`frozen_at`（2026-06-08 / 07-08 / 07-13）**均晚于 Q2 基线** → 漂移 = **有记录的再冻结**，
**非**静默编辑。冻结治理在本期**成立**。

### 5.2 未冻结的必停面（9 runtime SKILL.md + `system.md`）

| Surface | vs Q2 |
|---|---|
| `src/mj_agent/skills/safe-sql-analysis/SKILL.md` | **clean**（逐字复现） |
| `src/mj_agent/skills/biz-domain-context/SKILL.md` | **clean**（逐字复现） |
| `src/mj_agent/skills/qcm-analysis/SKILL.md` | **clean**（逐字复现） |
| `src/mj_agent/prompts/system.md` | **clean**（逐字复现） |
| `src/mj_agent/skills/biz-schema-exploration/SKILL.md` | 新入账 → baseline-only |
| `src/mj_agent/skills/mj-ddd-semantics/SKILL.md` | 新入账 → baseline-only |
| `src/mj_agent/skills/monthly-report/SKILL.md` | 新入账 → baseline-only |
| `src/mj_agent/skills/query-optimization/SKILL.md` | 新入账 → baseline-only |
| `src/mj_agent/skills/query-writing/SKILL.md` | 新入账 → baseline-only |
| `src/mj_agent/skills/probe-fixture/SKILL.md` | 新入账 → baseline-only |

> 这 6 面**此前从未被任何 cycle 快照**，却 5 面真实进 LLM prompt（`agent.py` `_ACTIVE_SKILLS`）
> 且 10 面全受 `ask` 拍板门保护 —— 正是 D1 拍板扩面的动因（见 §6 F6）。
> `probe-fixture` 不在 `_ACTIVE_SKILLS`（#334 P2 测试 fixture，不入 prompt），但**受同一
> `ask` glob 保护**且是 markdown → 按「**ask 门所护的 markdown 面**」口径入账。

### 5.3 `ask` 档另 3 个必停面 —— **故意不纳入本 hash 审计**（非遗漏）

`permissions.ask` 共 5 条，除上文 10 个 markdown 面（`skills/**/SKILL.md` ×9 + `system.md`）外，
另 3 条**是必停面但非 AI-context markdown 面**，按 `SCHEMA.md §2.1` 口径**不入** `content_hash_snapshot`：

| 必停面 | 类型 | 为何不入本审计 | 其 drift 由谁监控 |
|---|---|---|---|
| `tools/sql/guardrail.py` | 代码 | regex-strip-frontmatter 算法对 `.py` 无意义 | `sql-guardrail-relax` 必停门 + 单测 |
| `tools/sql/precheck.py` | 代码 | 同上 | 同上 |
| `biz_catalog/qcm_catalog.yaml` | 数据 | 纯 `.yaml` 无 frontmatter/body 之分 | `biz-catalog-sync` 必停门 |

> 故本审计的 23 面 = 「ask 门所护的 markdown 面（10）+ 冻结 infra（8）+ CLAUDE.md（5）」，
> **不等于** ask 门所护的**全部** 5 类面（后者含上述 3 个非-markdown 必停面）。这 3 面**已被各自
> 专属机制监控**，此处仅记一笔以证「非遗漏」。**D1 拍板的「门所护之面」= markdown 口径**（Owner
> 选项枚举逐字含且仅含这 23 面；`.py`/`.yaml` 3 面从未在 15/17/22/23 任一选项内）。

## §6 Findings

| # | Finding | 判定 |
|---|---|---|
| **F1** | 冻结面 **8/8 drift-clean vs contract**，零违规；6 面 vs Q2 的漂移全部由已记录的再冻结解释（`frozen_at` 均晚于 Q2） | ✅ 治理成立 |
| **F2** | 4 面（3 runtime skills + `system.md`）自 Q2 **逐字未变** —— 同时充当本期算法自证 | ✅ 无漂移 |
| **F3** | **`SCHEMA.md` 自身的构成规则已腿**：`§2` 写死「15-surface」、必停轨枚举「3 runtime + 6 infra」，而磁盘为 9 runtime / 8 infra。#304 冻结 `app-start`/`app-stop` 时无人回改本文件，**无 gate 可捕** | ⚠️ **本期同 PR 修复**：`§2.1` 改为推导规则（面集从 `ask` glob + contract + `git ls-files` 现场推导，数量降为观测值） |
| **F4** | 5/5 CLAUDE.md 相对 Q2 漂移（根 610→170 行）—— 主因 M6 SDD 重构（#245）把 CLAUDE.md 指针化 | ✅ 预期内；CLAUDE.md 无冻结机制，本期记新基线 |
| **F5** | 路径重命名 `infra/docker/CLAUDE.md` → `docker/CLAUDE.md`（Phase M5）—— Q2 entry `:51` 已预告该 rename | ✅ 已对应，hash 照常比对 |
| **F6** | 面集 **15 → 23**（+8）：+5 已激活但从未入账的 runtime skill、+`probe-fixture`、+2 冻结 infra（#304）。8 新面无 Q2 基线 → **baseline-only**，drift 判定自 Q4 起 | ✅ Owner D1 拍板（#347） |
| **F7** | **A6 提醒机制已静默失效**：`SCHEMA.md:54` + `2026-Q2.md:145` 均要求「register `M-FU-AI-AUDIT-2026-Q3` after 2026-06-30」，但该 M-FU **从未注册**（`ls plans/` 无此工件）→ Q3 逾期 15 日。**讽刺点**：SCHEMA §3 选择 M-FU 提醒而非 CI cron，理由正是「cron silently lapses without ownership acknowledgement」—— 结果 M-FU 机制自身以同样方式静默失效 | ⚠️ **本期同 PR 缓解**：补注册 Q3（逾期）+ Q4；**残余风险**见 §7 |
| **F8** | `policies/ci-gates.md` 未同步 ADR-034：`:67` 仍称必停面 = `deny` 硬锁、`:68` 把 `allow` 归 `settings.local.json`（实况 `settings.json` 持 24 条）、`:88`（A13 **PR 阻塞条件**）引用该失效定义、`:38`（**本审计自身的 scope 条款**）只列 `deny` 未提 `ask` | ⚠️ **本期同 PR 修复**（F 半）；`:38` 修正后本审计的 scope 才与实际覆盖面自洽 |
| **F9** | `settings.json` 结构变化：新增 `ask` 档 5 条（Q2 schema 无此概念）；`allow` 8→24；`deny` 6→9 且**不再含**任何必停文件 | ✅ = ADR-034 预期终态 |
| **F10** | hooks 与 `enabledPlugins` 自 Q2 **零漂移**（1 PreToolUse + 1 Stop；`enabledPlugins` 恒空） | ✅ 无漂移 |
| **F11** | skills 清点 34 → 37（flow +1、infra +2） | ✅ 记录 |

## §7 #344 保留项判据 —— E1/E2 锚点基线

> 本节履行 `plans/[PLAN]_dual-agent-compat_settings-narrow.md:83` 的逐字要求：
> 「**逾期的 Q3 审计 = 基线快照记录者，不是窗口关闭者**：它须记录本切片的 E1/E2 锚点值」。
> **窗口关闭者 = 2026-Q4 审计**（≈2026-10-01），届时复测同两项并作二值判定
> `biz_devtest_allow_used: yes/no`。

**窗口锚点** = PR #345 merge commit `07e1be6`（2026-07-16 14:06:56 +0800）。

> **两项证据缺一不可**（承 `plans/[PLAN]_dual-agent-compat_settings-narrow.md` §诚实边界）：
> **E1** = 真实调用计数（尽力而为、机器本地）；**E2** = 仓内不变量（权威、可 CI 复核）。
> E1 只证「历史上调用了几次」、E2 只证「无自动路径依赖」，互补。

### E1 — 真实调用计数（尽力而为、本机）

```bash
# 逐 server（匹配 #344 基线格式）
for s in dev test-lan test-wan; do
  grep -rl "\"name\":\"mcp__pg-mj-system-biz-${s}__" ~/.claude/projects/ | wc -l
done
# 三 server 合并 distinct 文件
grep -rl '"name":"mcp__pg-mj-system-biz-\(dev\|test-lan\|test-wan\)__' ~/.claude/projects/ | wc -l
```

| 指标 | 2026-Q3 锚点值 | #344 基线（2026-07-16）|
|---|---:|---|
| `biz-dev__query` | **1** | 1 |
| `biz-test-lan__query` | **2** | 2 |
| `biz-test-wan__*` | **0** | 0 |
| **合并 distinct 文件（精确 `tool_use` pattern）** | **2** | 2 |
| 对照：裸名 grep（`mcp__pg-mj-system-biz-dev__`） | **1136** | 1077 |

> **判据自身的一部分 —— 必须用精确 pattern**：裸名 grep 会被 agent 讨论正文污染，
> 本期实测假阳比 ≈ **568×**（1136 vs 2）。#344 落盘时为 1077 vs 2（≈538×）——
> 分母随 transcript 累积增长而扩大，**精确值恒为 2**，佐证 pattern 纪律有效。
> **口径警告**：E1 是**机器本地**证据（`~/.claude/projects/`，仓外、不可从仓内复现），
> 且 transcript 可能被轮转/清理 → Q4 复测须在**同一台**机器（= 本切片执行机，Owner 的
> Windows 开发机）上执行；若届时 transcript 已被清理，须在 Q4 entry 明记「E1 不可测」
> 而**不得**以 0 冒充零调用。

### E2 — 仓内不变量（权威、可 CI 复核）

```bash
grep -rln "settings\.json" scripts/ .github/ tests/ --include=*.py | wc -l
```

| 指标 | 2026-Q3 锚点值 | #344 基线 |
|---|---:|---|
| **E2：读 `settings.json` 的 `.py` 文件数** | **0** | 0 |

> **反例警告（承 #344）**：勿用「biz server 名」计数做此不变量——`grep -rn "pg-mj-system-biz-dev"`
> 等今日命中 6 处，但**全部**是 `check_development_agent.py` 的 `MCP_FORCED_NEVER` 常量 +
> `.mcp.json` projection fixture —— **同名不同面**，与 settings allow 无关。E2 = **0** 证明
> 无任何 `.py` 自动依赖 `settings.json` 内容（即删三条 allow 不会破坏任何仓内自动路径）。

### 保留面清单（context）

`settings.json:22-24` 三条（#345 后）：
`mcp__pg-mj-system-biz-dev__*` · `mcp__pg-mj-system-biz-test-lan__*` · `mcp__pg-mj-system-biz-test-wan__*`

已删（#345，A′ 拍板）：`mcp__pg-mj-system-biz-prod-lan__*` · `mcp__pg-mj-system-biz-prod-wan__*`
→ biz 子集 5→3、allow 26→24。

### 残余风险（Owner 已明示接受）

`#344` 的退出判据以 **2026-Q4 审计**为关闭者，而本期 F7 已**实证**该提醒机制会静默失效。
补注册 Q4 M-FU 是**缓解**、不是**保证**：若 Q4 提醒亦失效，该判据将永不触发。
留档见 `plans/[INTAKE]_dual-agent-compat_settings-narrow.md` §7.2。

## §8 下一 cycle

- **Trigger**：2026-Q4 季度自然边界（≈**2026-10-01**）。
- **已注册提醒**：`plans/[PLAN]_m-fu-ai-audit-2026-Q4.md`（**本期新增** —— 且为 #344 判据的关闭者）。
- **Q4 必办**：① 复测 E1/E2 并作 `biz_devtest_allow_used` 二值判定（§7）；
  ② 8 个 baseline-only 面首次可作 drift 判定；③ 按 `SCHEMA.md §2.1` **重新推导**面集
  （勿照抄本期的 23）。
