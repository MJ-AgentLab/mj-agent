# ========== Python ==========
__pycache__/
*.py[cod]
*.pyo
*.egg-info/
*.egg
dist/
build/
wheels/

# ========== 虚拟环境 ==========
.venv/
venv/
env/

# ========== 环境配置（敏感信息）==========
.env
.env.local
.env.*.local

# ========== 加密配置（解密后的明文）==========
config/secrets.conf
config/secrets-mcp.conf

# ========== IDE ==========
.idea/
.vscode/
*.swp
*.swo

# ========== Claude Code 本地配置 ==========
.claude/settings.local.json

# ========== Serena 本地配置 ==========
.serena/

# ========== OS ==========
Thumbs.db
.DS_Store
desktop.ini

# ========== 临时文件 ==========
*.tmp
*.bak
nul

# ========== 日志 ==========
*.log

# ========== Python 测试/类型/Lint 缓存（mj-agent 独有）==========
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# ========== LangGraph Studio（mj-agent 独有）==========
.langgraph_api/

# ========== Chainlit（auto-generated config + translations）==========
# `.chainlit/` is regenerated on first `chainlit run` from defaults; we
# only commit it back when project-specific UI customization is added.
.chainlit/
chainlit.md

# ========== Git worktrees（并行 agent 隔离） ==========
.worktrees/

# ========== 本地 agent 工作区（Epic #499 PR-0c；绝不入库） ==========
# `.mj-agent-local/biz-schema-snapshots/` 存放 Owner 背书的 sanitized biz schema
# 快照，供 `scripts/diff_biz_schema.py` 离线做 catalog 漂移检测。快照来自业务仓
# 元数据，**永不提交**——入库即等同把 biz schema 泄漏进公共历史（ADR-006 / ADR-009）。
.mj-agent-local/

# ========== 学习材料（本地草稿，不入库） ==========
learning/
