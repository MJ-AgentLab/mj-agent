"""Agent state placeholder.

Phase 0 relies on LangChain's default AgentState (messages only); this file
reserves the symbol for Phase 1 where custom state (e.g. analyst persona,
tool telemetry) is expected.
"""
